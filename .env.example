# Open WebUI Configuration
# ========================

# Secret key for session encryption
# Generate with: openssl rand -hex 32
WEBUI_SECRET_KEY=your-secret-key-here

# Increase streaming buffer size to prevent "chunk too big" errors
# This is especially useful when using fast models like Gemini
# Default is usually 1000, increase for faster models
CHAT_STREAM_RESPONSE_CHUNK_MAX_BUFFER_SIZE=10000
