---
name: LM Studio Start
description: Start LM Studio server in headless mode
mode: terminal
---

Start the LM Studio HTTP server with CORS enabled:

```bash
lms server start --port 1234 --bind 0.0.0.0 --cors
```
